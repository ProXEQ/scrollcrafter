"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};
var fs_exports = {};
__export(fs_exports, {
  parseFileContent: () => parseFileContent,
  writePo: () => writePo
});
module.exports = __toCommonJS(fs_exports);
var import_promises = __toESM(require("fs/promises"));
var import_block = require("./block.js");
var import_utils = require("./utils.js");
function readBlock(lines) {
  const block = [];
  let line = lines.pop();
  while (line) {
    block.push(line);
    line = lines.pop();
  }
  return [block, lines];
}
__name(readBlock, "readBlock");
function readBlocks(lines) {
  const blocks = [];
  while (lines.length > 0) {
    const res = readBlock(lines);
    const b = new import_block.Block(res[0]);
    blocks.push(b);
    lines = res[1];
  }
  return blocks;
}
__name(readBlocks, "readBlocks");
function parseFileContent(data) {
  const lines = data.split(/\r?\n/).reverse();
  const blocks = readBlocks(lines);
  return blocks.sort(import_utils.hashCompare);
}
__name(parseFileContent, "parseFileContent");
function writePo(header, blocks, output) {
  return __async(this, null, function* () {
    let consolidated = header ? header.toStr() + "\n\n\n" : "";
    consolidated += blocks.cleanup().toStr();
    yield import_promises.default.writeFile(output, consolidated, { encoding: "utf8", flag: "w" });
    return consolidated;
  });
}
__name(writePo, "writePo");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  parseFileContent,
  writePo
});
